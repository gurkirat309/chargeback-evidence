# Data — raw IEEE-CIS is huge and licensed; keep it out.
data/raw/
# processed parquet (336 KB) + warmed LLM cache (2.3 MB) ARE committed so the
# deployed app runs with no raw CSVs and no live LLM calls. See render.yaml.

# Kaggle CSVs (some sit at repo root); never track raw/generated CSV
*.csv
!config/*.csv

# Environments
.venv/
venv/
env/
__pycache__/
*.pyc

# Generated reports / plots
reports/

# Secrets — never commit API keys
.env
.env.*
*.key
secrets.*

# Pitch / video scripts — local only, not for the judged repo
docs/PITCH.md
docs/VIDEO_SCRIPT.md
video_script.txt
video_script.md

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
